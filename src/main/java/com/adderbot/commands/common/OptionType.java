package com.adderbot.commands.common;

public enum OptionType {
    /**
     * Unused, left to keep in sync with Discord4J's option type enum
     */
    SUB_COMMAND,
    /**
     * Unused, left to keep in sync with Discord4J's option type enum
     */
    SUB_COMMAND_GROUP,
    STRING,
    INTEGER,
    BOOLEAN,
    USER,
    CHANNEL,
    ROLE,
    MENTIONABLE,
    NUMBER
}
